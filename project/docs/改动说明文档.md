# 视觉风格优化 · 改动说明文档

> 分支:`genspark_ai_developer` | 技术栈:Vue 2.6 + Element UI 2.15 | 设计基调:年轻明亮、轻科技感、精致高级
> 配置侧表格设计语言参考 https://ease-ui.com/data/table(经 Playwright 实测取值:表头 #f8fafc / 文字 #66758a / 13px / 600 / 边框 #d7e0ea)

---

## 一、新增文件

### 1. `src/styles/design-tokens.scss`(设计令牌系统)
- 全局 CSS 变量:品牌色 `--brand: #2b5cff`、渐变 `--accent-gradient (3b82f6→06b6d4)`
- ease-ui 表格令牌:`--table-th-bg/--table-th-color/--table-th-border(#d7e0ea)/--table-td-color`
- 圆角、阴影梯度(`--shadow-card`)、字体(`--font-mono` 等宽代码字体)
- 响应式基础:rem/clamp() 变量,适配 1366/1440/1920/2560

### 2. `src/styles/element-override.scss`(Element UI 全局覆盖)
- 按钮、输入框、下拉、弹窗、抽屉、提示、分页等组件统一为令牌风格
- 圆角统一、阴影减淡、主色替换为 #2b5cff,消除老旧感

### 3. `src/components/BaseIcon/index.vue`(全新图标库)
- 全局注册的 SVG 线型图标组件(lucide 视觉语言,24×24 stroke,~65 个图标)
- props: `{ name, size=16, strokeWidth=1.8 }`,用于替换全项目 Element 字体图标
- 已覆盖:search/plus/refresh/edit/trash/download/copy/send/database/layers/history/sparkles/bot/brain/lightbulb/bar-chart/trending-up/file-code 等

### 4. `src/components/SqlPanel/index.vue` + `src/utils/sqlHighlight.js`(专业 SQL 展示)
- 只读 SQL 查看器:头部标题 + SQL 徽章 + 一键复制按钮
- 左侧行号槽(gutter)+ 浅色主题语法高亮(正则分词:关键字/函数/字符串/数字/注释)
- 等宽字体,专业代码编辑器观感

### 5. `src/mock/index.js`(仅开发环境 Mock 层)
- axios 请求拦截器注入自定义 adapter,~25 条路由(登录/智能体/历史会话/chatInfo/预览/配置侧列表)
- `window.WebSocket` 包装:对 `/api/websocket/` 路径返回 MockWebSocket(流式思考 → metadata.close)
- 仅 `NODE_ENV === 'development'` 时通过 `require('@/mock')` 启用,**不影响生产构建**

## 二、修改文件

### 6. `src/main.js`
- 引入 design-tokens.scss / element-override.scss(先于 global.scss)
- 全局注册 `BaseIcon`;dev-only mock require

### 7. `src/assets/css/global.scss`(整体重写,保留全部旧类名,业务零改动)
- 顶部标签页改为胶囊 pill 风格(32px 高、激活态品牌描边+阴影)
- `.tablebg` 改为白色圆角卡片(圆角/描边/卡片阴影/16px 外边距)
- `@mixin easeui-table` 应用到 `.drag-table` / `.normal-table`:表头 #f8fafc、文字 #66758a、13px、行高 40px、悬浮 #f4f7ff
- `.searchBox` gap 布局、`.vertical-line` 渐变、`.warntext-bg` 蓝色信息条
- 新增 `.op-icon-btn`(表格操作列图标按钮:6px 内边距、悬浮浅蓝底、`is-danger` 红色变体)

### 8. `src/views/newBI/index.vue`(问数主页框架)
- 明亮背景:三层径向渐变(蓝/青/紫,alpha 0.05–0.08)+ 线性 #fbfcff→#f4f7fd
- 侧栏 `clamp(220px, 16vw, 280px)`,白色 66% 半透明 + backdrop-blur

### 9. `src/views/newBI/aiBodyPage/index.vue`(智能体列表·重写)
- 渐变徽章头部 + 虚线新建按钮;列表项 sparkles 头像、悬浮显现编辑/删除、激活态高亮、空状态
- 全部方法/emit 保持原样(getAiBodyList/selectAgent/deleteAgent/changeAgent 等)

### 10. `src/views/newBI/aiHistoryPage/index.vue`(历史记录·重写)
- 紫色渐变徽章头部;扁平分组标签(今天/昨天/…)+ message-plus 图标行,悬浮品牌色
- 移除 el-collapse;新增 `isAllEmpty` 计算属性;方法/emit 不变

### 11. `src/views/newBI/queryPage/index.vue`(问数主体)
- 欢迎页 `.welcome-hero`:56px 渐变徽章 + clamp 标题(智能体名渐变字)+ 3 个提示 chips
- SQL 弹窗改用 `<sql-panel>`(语法高亮+行号+复制),宽 640px
- 图标替换:el-icon-tickets→file-code、download、send(渐变发送按钮 40×36 悬浮上浮)
- 思考徽章紫色渐变 brain 图标;步骤徽章 compute(青)/analyze(紫)/summary(绿)胶囊
- 用户气泡渐变 14px 圆角、AI 面板白色 92% + blur、输入区 16px 圆角 focus 品牌描边
- **全部业务逻辑(praseAnswer/WS 链路/筛选/分页)未动**;修复 analyze 徽章缺分号 CSS bug

### 12. `src/views/dataSourceManager/index.vue`(部分,操作列图标替换进行中)

## 三、验证情况
- 编译:`DONE Compiled successfully`(全程无 error)
- Playwright 截图 E2E:登录 → 问数 → 提问 → 思考流 → 查询表格 → 分析/总结 → SQL 弹窗,1920 与 1366 双分辨率通过
- 管理端(数据源管理)截图:白卡片 + ease-ui 表头/分页已由 global.scss 全局生效

---

# 第二轮迭代(2026-07-14)

## 五、问题修复(BUG)

### 13. 智能体配置弹窗被挤压在侧栏内(用户截图 1)
- **文件**:`src/views/newBI/aiBodyPage/index.vue`
- **根因**:el-dialog 渲染在窄的智能体侧栏(flex 子容器)内部,弹窗被父容器宽度约束挤压变形,无法操作
- **修复**:为 el-dialog 增加 `append-to-body`(挂载到 body,脱离侧栏布局),并设定 `width="880px" top="8vh" :close-on-click-modal="false"`,统一使用 `my-custom-style` 弹窗样式
- 已验证:点击 + 新建/编辑智能体,弹窗居中全屏遮罩正常展示

### 14. 数据源弹窗 JDBC URL 显示 `undefined:undefined/`
- **文件**:`src/views/dataSourceManager/newDataSourcePage/index.vue`
- `JDBCSplice` 计算属性对未初始化字段直接拼接导致 undefined 字样;改为空值兜底(`host/port/defaultDb || ""`)

### 15. newAiBodyPage 缺失 `deleteMetric` 方法(潜在运行时报错)
- 模板中知识行删除按钮引用了 `deleteMetric(row)` 但方法从未实现,点击必报错
- 已补齐实现:从 `tableRelations[i].relations` 移除该行、清理空分组、并通过 `toggleRowSelection` 同步取消左侧表格勾选

## 六、新增/优化(第二轮)

### 16. `src/components/TopNav/index.vue` — 应用端/管理端切换重设计(用户截图 2)
- 原裸文字按钮 → **分段式胶囊开关(segmented control)**:容器 #f1f4f9 圆角 999px,激活项白底 + 品牌色 + 阴影,带 message-circle / settings 图标
- 新增 `activeSide` 状态(默认 1 = 应用端,与父级 `page: 1` 一致),点击切换高亮并 emit openPage
- 用户头像 el-avatar → 渐变圆形 `.top-nav__avatar`;下拉箭头 → BaseIcon chevron-down

### 17. `src/components/SqlEditor/index.vue`(新建)— 可编辑 SQL 编辑器(用户截图 3)
- 头部:SQL 渐变徽章 + 标题 + 实时"N 行 · N 字符"统计
- 左侧行号沟槽(与文本滚动同步);等宽字体 12.5px/21px;**Tab 键插入 2 空格**且保留光标位;focus 品牌色描边
- 支持 v-model(emit input),纯展示型组件、零业务侵入

### 18. `src/views/fieldMappingManager/viewRegisteredPage/index.vue`(注册视图弹窗·重写模板)
- 转换为 el-form(label 置顶):数据源下拉、视图英文名/中文名两列
- 视图 SQL 接入 `<sql-editor>`(行号 + 统计 + Tab 缩进),替代裸 textarea
- "执行并解析"按钮带 play 图标;解析结果表格字段状态胶囊:保留(绿)/新增(蓝)/删除(红)
- 全部方法(sureView/parseTableSql/getViewDetal/onDragEnd 等)未动

### 19. `src/views/newBI/newAiBodyPage/index.vue`(智能体配置弹窗·重写模板)
- el-form 顶部行(智能体名称/描述);tabs 带图标(数据表/行业知识)
- 双栏面板布局:渐变竖条标题、左栏工具条(数据源下拉 + 搜索框)、知识行(渐变序号圆 + 输入框 + 删除图标按钮)
- 底部:添加(plus 图标,仅知识 tab)+ 取消/确定

### 20. 配置侧 6 个主页面操作列图标批量替换(op-icon-btn 模式)
- 涉及:`dimensionManager` / `metricDataManager` / `portfolioManager` / `fieldMappingManager` / `dataModelManager` / `metricDataPreview`
- `el-icon-refresh` → BaseIcon refresh;`el-icon-plus` → BaseIcon plus;操作列文字按钮 → `.op-icon-btn`(edit/trash/sort-asc/sort-desc/settings),v-if 条件全部保留
- 保留功能性图标:loading/search/date/caret/warning/close/arrow/view

### 21. `src/views/metricDataPreview/index.vue` — SQL 弹窗接入 SqlPanel
- 原黑底纯文本 → `<sql-panel>`(语法高亮 + 行号 + 复制),弹窗标题"查询 SQL"、宽 640px、内容区 max-height 56vh 可滚动
- SQL 按钮加 file-code 图标、下载按钮加 download 图标、重置按钮加 refresh 图标

### 22. `src/views/dataSourceManager/newDataSourcePage/index.vue`(数据源弹窗·el-form 转换,S1 代表样例)
- 分区标题"基本信息 · 连接配置"(渐变竖条);两列行:名称/类型、主机/端口、库/Schema、用户名/密码
- JDBC URL 等宽字体卡片;测试成功绿色卡片(check-circle 图标);底栏:测试连接(zap 图标)居左,取消/创建居右

### 23. `src/components/Pagination/index.vue` — 细化
- "共 N 条数据"文案 12.5px #66758a;移除遗留的 34px 内联 fontSize

### 24. `src/mock/index.js` — 在线预览假数据补充(用户诉求 5)
- 问数主链路假数据此前已就绪(登录 demo/demo、智能体列表、历史记录、MockWebSocket 推送思考/查数/分析/总结全流程)
- 本轮新增:智能体候选表(4 张演示表)、字段映射数据源列表、**注册视图 SQL 解析**(POST parse 返回 4 个字段含保留/新增状态)、注册视图提交

## 七、验证情况(第二轮)
- 编译:全程 `DONE Compiled successfully`,无 error
- Playwright E2E 截图验证(1920):TopNav 胶囊开关 ✅、智能体配置弹窗居中 ✅、问数全流程(思考/查数/分析/总结 + 结论文本)✅、注册视图 SqlEditor(行号/多行 SQL)✅
- 响应式补充:1440×900 问数对话流 ✅、2560×1300 欢迎页 ✅、2560 管理端 + 新建数据源 el-form 弹窗 ✅

## 八、剩余待办(建议后续迭代)
1. 其余约 11 个配置侧弹窗逐一 el-form 转换(collectPage / metaDataPage / collectHistoryPage / newDataModelPage / newDimensionPage / newMetricDataPage / newPortfolioPage / physicalRegisteredPage / semanticRegistration / filterPage / sortPage)——已完成的 newDataSourcePage / viewRegisteredPage / newAiBodyPage 可作为直接照搬的模板
2. 如需生产环境部署,`src/mock` 仅在开发模式启用(`process.env.NODE_ENV === "development"`),打包后自动走真实接口,无需删除

---

# 第三轮迭代(2026-07-14)

## 九、主问数界面深度优化

### 25. 思考过程框条 — 水波纹光晕动画(诉求 1-1)
`src/views/newBI/queryPage/index.vue`
- **思考中(`.aiDisableCollapse`)**:表头改为多段柔和渐变底色,`background-size: 220% 100%` 配合 `@keyframes think-wave` 让渐变背景左右流动,如水波纹;叠加 `::after` 白色高光斜向扫过(`@keyframes think-shine`),形成"正在推理"的光晕呼吸感
- **思考完成(`.aiThinkCollapse`)**:静态紫蓝渐变 `#f5f3ff → #eef2ff → #f0f9ff`,淡紫描边、12px 圆角,悬停微抬升 + 阴影
- 思考内容区抽出 `.think-content` 类,底色为三段浅紫→浅蓝渐变

### 26. 查数/分析/总结 → 1/2/3 序号(诉求 1-2)
- `praseAnswer()` 数据整形阶段引入 `stepNo` 计数器,对每个回答块写入 `obj.stepNo = ++stepNo`
- 模板四处徽标统一改为 `{{ chatItemInfo.stepNo || chatItemInfo.itemType }}`,左侧徽标显示 1 / 2 / 3 步骤序号,保留渐变圆角方块底

### 27. 问数结果框条颜色刷新 + 悬停效果(诉求 1-3 / 1-7)
- `.aiCollapse` 表头:清爽蓝青渐变 `#f7faff → #f4f8ff → #f2fbfd`,替换原陈旧灰底
- `.aiAnalysisCollapse` 表头:蓝紫渐变
- 全部结果框条悬停:`translateY(-2px)` 抬升 + `0 6px 18px rgba(43,92,255,.13)` 品牌色阴影,过渡平滑

### 28. 维度/指标筛选标签大圆角(诉求 1-4)
- `.queryDimPanel`:`border-radius: 999px` 全圆角胶囊,浅蓝渐变底 `#e3f1fe → #dbf3fd`,悬停微抬升

### 29. 总结/分析文字渐变质感底色(诉求 1-5)
- 原内联单色底 div 抽象为 `.ai-result-card` 组件类:
  - `--analyze` 分析卡:蓝→紫四段渐变 `#f0f7ff → … → #f6f1ff`,靛蓝描边
  - `--summary` 总结卡:蓝→青四段渐变 `#eff8ff → … → #f3fcff`,翠绿描边
- 均叠加 `inset 0 1px 0 rgba(255,255,255,.85)` 顶部高光,营造玻璃质感

### 30. 问数输入框双层修复(诉求 1-6)
- 点击问数(禁用态)时 el-textarea 内层仍保留边框/底色,形成两层视觉
- 修复:`.aiInput-textarea ::v-deep .el-textarea__inner`(含 `.is-disabled` / `:disabled`)强制 `border:none / background:transparent / box-shadow:none`,并用 `-webkit-text-fill-color` 保持文字颜色,任何状态下只呈现外层一个容器

### 31. 用户对话气泡渐变质感(诉求 1-8)
- `.userPanel`:四段渐变 `#e8efff → #e3f0ff 40% → #ddf3ff 75% → #e6f0ff`,顶部内嵌白色高光,`16px 4px 16px 16px` 不对称圆角,呈现轻拟物质感

## 十、智能体配置布局松弛化(诉求 2)

### 32. `src/views/newBI/newAiBodyPage/index.vue` + `aiBodyPage/index.vue`
- 弹窗宽度 880px → 960px
- 根容器 `padding: 4px 28px 20px`;表单标签下边距 6px、表单项间距 20px
- Tabs 头部下边距 18px;左右两栏面板间距 20px,面板内边距 18px、12px 圆角、`#fcfdff → #f9fbfe` 微渐变底
- 面板标题、工具行、知识行间距全面加大;底栏加分隔线 `border-top` + `padding: 18px 0 4px`

## 十一、管理端深度优化

### 33. SqlEditor 专业化重写(诉求 3-1,重点功能)
`src/components/SqlEditor/index.vue` 整体重写为**真正的语法高亮编辑器**(零依赖):
- **叠层高亮架构**:透明文字 textarea 叠在 `v-html` 高亮 `<pre>` 之上,两层字体度量完全一致(12.5px / 21px 等宽),滚动实时同步——既保留原生输入体验,又获得实时着色
- **语法高亮**:复用 `src/utils/sqlHighlight.js`,关键字/函数/字符串/数字/注释/标识符分色渲染,输入即着色
- **行号栏**:当前行(光标所在行)品牌色高亮
- **状态栏**:实时 `Ln 行, Col 列 · N 行 · N 字符` 等宽字体展示
- **格式化按钮**:一键 SQL 美化——FROM/WHERE/GROUP BY/ORDER BY/JOIN 系列关键字前断行,AND/OR/ON 缩进,SELECT 字段逗号换行
- **编辑器化细节**:macOS 三色圆点窗口装饰、SQL 渐变徽章、Tab 键插入 2 空格、聚焦态品牌色描边光圈
- 对外 props(value/title/placeholder/rows)不变,`v-model` 兼容,所有引用处零改动

### 34. 注册视图/数据源弹窗布局松弛化(诉求 3-1 / 3-3)
- `fieldMappingManager/index.vue`:注册视图弹窗 `60%` → 固定 `880px`
- `viewRegisteredPage/index.vue`:滚动区 `padding: 4px 28px`,表单项间距 22px,底栏分隔线 + `padding: 16px 28px 18px`
- `dataSourceManager/index.vue`:数据源弹窗 `60%` → `820px`、`top: 7vh`
- `newDataSourcePage/index.vue`:表单容器 `padding: 4px 28px 6px`,表单项间距 22px,分区标题下边距 20px,底栏分隔线

### 35. 全站渐变按钮体系(诉求 3-2)
`src/assets/css/global.scss` 全局追加:
- `.el-button--primary`:`linear-gradient(135deg, #3b82f6, #2b5cff 55%, #4f46e5)` 三段渐变,去边框
- `--danger` / `--success` 同规格渐变;`--default` 悬停品牌描边 + 浅渐变
- **悬停动画**:`translateY(-2px)` 弹起 + 更深品牌色阴影 `0 6px 16px rgba(43,92,255,.38)`,过渡曲线 `cubic-bezier(0.34,1.56,0.64,1)` 带轻微回弹;`--text` 文字按钮排除位移
- 禁用态改为淡渐变,保持一致质感

### 36. 输入框/背景全局打磨(诉求 3-4)
- `.el-input__inner / .el-textarea__inner`:默认 `#fbfcfe` 微灰底;悬停品牌描边 + 纯白底;聚焦品牌描边 + `0 0 0 3px rgba(43,92,255,.08)` 光圈
- 弹窗内 `el-form` 统一呼吸感:label 下边距 6px、表单项间距 20px

## 十二、验证情况(第三轮)
- 编译:全程 `DONE Compiled successfully`,零 error
- Playwright E2E 截图逐项验证:
  - 思考中水波纹渐变动画表头 ✅(v3_thinking)
  - 1/2/3 步骤序号徽标 + 渐变总结/分析卡片 ✅(v3_chat_full)
  - 维度/指标胶囊标签 ✅(v3_query_tags)
  - 智能体配置弹窗 960px 松弛布局 ✅(v3_agent_dialog)
  - SqlEditor 专业编辑器:关键字着色/行号/当前行高亮/Ln·Col 状态栏/格式化 ✅(v3_sql_editor)
  - 数据源弹窗 820px 松弛布局 + 渐变创建按钮 ✅(v3_ds_dialog)
- 线上预览:开发服务器热更新,所有改动已实时生效(登录 demo/demo)

---

# 第四轮迭代(2026-07-14)

## 十三、思考过程动画降噪(诉求 1)
`src/views/newBI/queryPage/index.vue`
- **颜色回归主色调**:思考中的水波纹渐变由"紫/靛/青多彩混合"改为**同色系蓝调**(`#f0f6ff → #e9f2ff → #e7f6fd`),描边统一为品牌蓝 `rgba(43,92,255,.14)`,与全站色调完全一致
- **放慢节奏**:背景流动 2.6s → **5.5s**,高光扫过 2.2s → **4.8s**,高光亮度 0.75 → 0.5,由"闪烁"变为"缓慢呼吸"
- 同步调整:思考完成态表头改为浅蓝渐变、`think-badge` 徽标由紫渐变改为品牌蓝渐变(`#4f8cff → #2b5cff`)、思考内容区底色同步蓝调

## 十四、添加筛选器卡片重设计(诉求 2)
- 原样式:无标题层级、贴边内容、底部按钮悬空
- 新结构 `filter-pop`:
  - **头部**:品牌蓝渐变图标徽章(filter 图标)+ 加粗标题,浅蓝渐变底 + 底部分隔线
  - **Tabs**:维度/指标选中项品牌蓝加粗,激活条改为渐变圆角(3px)
  - **选项列表**:每项整行可 hover(圆角浅蓝底),内边距加大,超长可滚动(max-height 180px)
  - **底部**:分隔线 + 浅蓝底操作栏,确定按钮 8px 圆角
  - 卡片整体:14px 大圆角、品牌色淡描边、柔和大阴影(`0 12px 32px`),宽度 264px

## 十五、配置端左侧导航整体优化(诉求 3)
`src/components/LeftMenu/index.vue`
- **容器**:白→浅灰蓝纵向渐变底,右侧品牌色淡分隔线,内容区加 10px 呼吸内边距
- **分组标题**(数据接入/模型与映射/…):13px 加粗、圆角 hover 浅蓝底、图标随 hover 变品牌蓝,箭头淡化
- **子项**:胶囊圆角(9px),hover 浅蓝底变色;**选中态**为品牌蓝渐变胶囊底 + 加粗 + **左侧 3px 渐变指示条**,层级一目了然
- 底部折叠按钮:圆角化 + 顶部分隔线;active-text-color 统一为品牌蓝 `#2b5cff`

## 十六、验证情况(第四轮)
- 编译:`DONE Compiled successfully`,零 error
- Playwright 截图验证:管理端导航新层级样式 ✅(v4_nav)、思考条同色系蓝调 ✅(v4_thinking)、添加筛选器新卡片(头部徽章/tabs/hover 行/底栏) ✅(v4_filter)
- 线上预览热更新已生效(登录 demo/demo)

---

# 第五轮迭代(2026-07-14)

## 十七、问数页 SQL 展示自动格式化(用户反馈:线上后端返回的单行 SQL 未被格式化)

### 问题定位
- 演示环境的 mock SQL 本身写成了多行,所以"看起来有格式化";而生产后端返回的是**单行长 SQL**,SqlPanel 只做语法高亮不做排版,于是原样单行展示

### 37. 新增 `src/utils/sqlFormat.js` — 零依赖通用 SQL 格式化器
- **完整词法切分**:字符串('…'/"…")、反引号标识符、行/块注释、括号、逗号逐一分词,保证任何内容不被拆断
- **括号深度感知**:`( SELECT …)` 子查询自动换行并整体缩进一级;函数括号(如 `sum(…)`、`DATE_FORMAT(…)`)保持行内
- **子句断行**:SELECT / FROM / WHERE / GROUP BY / ORDER BY / HAVING / LIMIT / OFFSET / UNION / 各类 JOIN 关键字换行并大写
- **细节排版**:SELECT 字段逗号换行缩进、AND/OR/ON 缩进换行
- **防误伤**:若 SQL 已有 ≥4 行且无超长行(≤120 字符),视为已手工排版,原样返回

### 38. `src/components/SqlPanel/index.vue` — 接入自动格式化
- 新增 `autoFormat` prop(默认开启):展示前先经 `formatSQL` 排版再语法高亮
- 复制按钮同步复制格式化后的 SQL
- 问数页 `showSQL()` 简化为直接透传原始 SQL;指标数据预览页同样受益(共用 SqlPanel)

### 验证(第五轮)
- 单元验证:用户提供的真实单行 SQL(嵌套子查询 + DATE_FORMAT + group by + limit offset)格式化输出 18 行,子查询正确缩进 ✅
- E2E:mock 改为同款单行 SQL,问数页"SQL"弹窗展示为完整排版 + 语法高亮 + 行号 ✅(v5_sql)
- 编译零 error;线上预览热更新已生效

---

# 第六轮迭代(2026-07-14)

## 十八、历史记录分组折叠(用户诉求:今天/昨天/上个月等分组可折叠,默认折叠)
`src/views/newBI/aiHistoryPage/index.vue`(已转 LF)
- **分组标题可点击折叠/展开**:左侧箭头(chevron-right)随展开旋转 90°,带过渡动画
- **默认全部折叠**:`expandedGroups` 初始为空;每次刷新历史列表后重置为全折叠
- **数量徽标**:标题右侧显示该分组的记录条数(浅蓝胶囊),折叠时也能一眼看到有多少条
- 标题 hover 变品牌蓝 + 浅蓝底,展开态标题加深;记录列表微缩进,层级清晰
- 交互实现:`isExpanded(key)` / `toggleGroup(key)`(`$set` 保证响应式),`v-show` 切换不销毁 DOM

### 验证(第六轮)
- 编译零 error
- Playwright 截图:默认折叠(今天 2 / 昨天 1 / 上周 1 徽标可见)✅(v6_history_collapsed);点击"今天"展开显示 2 条记录、箭头旋转 ✅(v6_history_expanded)

## 第七轮迭代（2026-07-14）

### 十九、分页组件换行修复（src/components/Pagination/index.vue）
39. **修复中间页码（如第5/6页）时"共N条数据"被挤压换行为两行的问题**。根因：分页器在中间页码时会同时渲染前后两个省略号、宽度变大，而总数文本没有 `white-space: nowrap` / `flex-shrink: 0` 保护，被压缩换行。
    - 移除模板中全部内联样式（含遗留的 `fontSize: '34px'`），改为语义化 class（`__inner` / `__total` / `__pager`）；
    - 总数文本：`white-space: nowrap; flex-shrink: 0`，永不换行、永不被挤压；
    - 分页器：`flex-wrap: nowrap; margin-left: auto`，内部元素单行展示；极窄屏时整个分页器换到下一行而不是内部折行。

### 二十、问数主页筛选条件区深度重构（src/views/newBI/queryPage/index.vue）
40. **去除全部硬编码布局**：原先"筛选器"行内 200+ 行内联样式（`width: 60px/100px/150px/250px`、`width: 90%`、三角形 × 删除按钮 hack 等）全部移除，改为语义化 class + scoped CSS，尺寸全局自适应（`flex: 1 1 auto; min-width: 0` + `max-width: 40vw/48vw` 上限 + 768px 断点）。
41. **胶囊式条件单元（.filter-cond）**：每个筛选条件（字段名 + 操作符 + 值 + 删除）合并为一个圆角胶囊卡片，内部控件去边框、由外层统一描边，hover 高亮、focus-within 品牌蓝描边；字段名超长自动省略并带 Tooltip。
42. **多条件布局**：`.filter-conds` 采用 `flex-wrap + gap: 10px 12px`，条件数量任意多时自动流式换行、间距一致，不再出现截图中的错位大空隙。
43. **删除按钮重做**：原"旋转正方形三角 + × 字符"的绝对定位 hack 替换为语义化 `<button class="filter-cond__remove">`（el-icon-close），悬停变红，视觉与交互都更清晰。

### 二十一、指标/维度标签超长处理（同文件）
44. **标签超长单行省略 + 悬停全文**：`.queryDimPanel__text` 设 `max-width: 168px; white-space: nowrap; overflow: hidden; text-overflow: ellipsis`，配合 `el-tooltip`（open-delay 150ms）悬停展示完整名称；"近七日暂降告警数量"等长名称不再把标签撑成两行。
45. 维度/指标两行的标签容器统一为 `.query-row__label`（固定 60px 不换行）+ `.query-row__tags`（flex-wrap + gap 8px + min-width: 0），与筛选器行共用一套行级布局。

### 二十二、Mock 数据增强（src/mock/index.js，仅开发预览用）
46. 新增长名称指标"近七日暂降告警数量 / 近七日短时中断告警数量"及 3 个预置筛选条件，用于复现并验证第 2、3 项场景；指标管理列表扩充到 99 条以验证多页码分页布局。
47. 修复 mock adapter 未读取 axios `config.params` 的问题（分页参数现可正确传入 handler）。

### 第七轮验证
- Playwright 截图：`shots/v7_filter_tags.png`（标签+筛选条件整体）、`shots/v7_tag_tooltip.png`（悬停 Tooltip）、`shots/v7_pagination_p5.png / p6.png`（含省略号的分页单行布局）。
- 编译零错误；所有改动仅涉及模板 class 与样式，业务逻辑（deleteFilter/operatorChange/getMetricsPreview 等）调用不变。

---

## 第八轮迭代（问数界面：标签长度上限与条件化 Tooltip、筛选器尺寸统一、日期区间分隔符）

### 二十三、指标/维度标签长度上限 + 条件化 Tooltip
48. 新增通用组件 `src/components/TruncateTip/index.vue`：
    - 内部为 `el-tooltip` 包裹单行省略文本（`white-space: nowrap; overflow: hidden; text-overflow: ellipsis`）；
    - 关键逻辑：鼠标移入时实时判断 `scrollWidth > clientWidth`，仅当文字**实际被截断**时才启用 Tooltip（`:disabled="!truncated"`）——短文本（如"销售额"）悬停不会出现任何气泡，长文本（如"近七日暂降告警数量"）悬停展示完整内容；
    - 支持 `text / maxWidth / placement / openDelay` 属性，可在全站复用。
49. 问数界面维度、指标标签文本（`.queryDimPanel__text`）改用 TruncateTip 渲染，并将最大宽度上限收紧为 `7em`（约 7 个汉字），超出部分省略号隐藏，标签整体更紧凑。

### 二十四、筛选器各部分尺寸固定、总长统一
50. 筛选器胶囊内各区块改为**固定宽度**（不再随内容或视口伸缩）：
    - 字段名 `.filter-cond__name`：固定 108px，超长文字同样使用 TruncateTip 条件化省略+悬停展示；
    - 操作符下拉 `.filter-cond__op`：84px；
    - 值输入框 `.filter-cond__value`：110px（修复此前 ">10" 的数字输入框过长的问题）；
    - 删除按钮 `.filter-cond__remove`：26px。
    → 动态筛选器胶囊总长恒定一致（108+84+110+26），多行排列时整齐对齐。
51. 日期类筛选条件同步固定：粒度选择 `.filter-cond__field` 88px、日期区间 `.filter-cond__date` 230px，且 `flex: 0 0 auto` 防止被压缩；移除原有 vw 上限与移动端多余覆盖，小屏仅保留 `flex-wrap` 换行。

### 二十五、日期区间分隔符布局修复
52. `el-date-picker`（daterange）增加 `range-separator="至"`，替换原来悬浮偏上的 "~"；
53. 通过 `::v-deep` 重排区间选择器内部布局：分隔符改为 `inline-flex` 垂直水平居中（宽度 10%、字号 12px、行高 30px），起止输入框各 42% 宽，日历/清除图标行高对齐 30px——"2026-01-01 至 2026-06-30" 整体居中对齐、不再错位拥挤。

### 第八轮验证
- Playwright 截图：`shots/v8_filter_tags.png`（标签 7em 截断 + 两行筛选器胶囊等宽对齐）、`shots/v8_tag_tooltip_long.png`（长文本悬停出现 Tooltip）、`shots/v8_tag_tooltip_short.png`（短文本悬停无 Tooltip）、`shots/v8_date_range.png`（"至"分隔符居中）。
- 编译零错误；改动仅为模板展示层（TruncateTip 替换纯文本节点）与 scoped 样式，deleteFilter / operatorChange / 查询请求等业务逻辑不变。

---

## 第九轮迭代（筛选器：时间筛选器尺寸统一、多选维度值收纳展示）

### 二十六、时间筛选器与其他筛选器总长统一
54. 日期区间宽度由 230px 调整为 240px，与粒度选择（88px）合计 328px（+边框），使时间筛选器胶囊总宽与动态条件胶囊（108+84+110+26=328）**完全一致**——Playwright 实测四个胶囊 bounding box 宽度均为 330px。

### 二十七、多选维度值过多时的收纳展示
55. 多选下拉输入框高度锁定 30px（`.filter-cond__value--select .el-input__inner`），不随标签数量变高，杜绝"华东 +3"竖向撑破胶囊的问题；
56. `.el-select__tags` 强制单行 `flex-wrap: nowrap` + `overflow: hidden`，高度 28px；
57. 收纳标签（el-tag）重设样式：高度 20px、内边距收紧、文本最大 36px 超出省略、品牌蓝浅底（#eef3ff/#2b5cff），"+N" 计数标签与首个值标签并排单行展示，关闭按钮悬停高亮；
58. mock 预置"地区 包含"筛选值扩展为 4 项（华东/华南/华北/西南），用于验证多值收纳效果。

### 第九轮验证
- Playwright 截图：`shots/v9_filters.png`（四个筛选器胶囊等宽单行排列，时间筛选器与其他一致）、`shots/v9_multiselect.png`（多选值"华东 × +3"单行收纳）。
- 实测胶囊宽度：`[330, 330, 330, 330]`；编译零错误，业务逻辑未改动。
